
# MongoDB Connection String
# Replace <username>, <password>, and <cluster-url> with your actual MongoDB Atlas credentials
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/unmask-protocol

# JWT Secret for Authentication
# Use a long, random string in production - at least 32 characters
JWT_SECRET=your-very-long-and-secure-secret-key-here

# Encryption Key for PII (Personally Identifiable Information)
# Use a secure, random encryption key - must be the same length as AES key (32 characters for AES-256)
ENCRYPTION_KEY=your-32-character-secure-encryption-key

# Server Port
# Default is 5000, but can be changed if needed
PORT=5000

# Optional: Additional environment-specific configurations
# NODE_ENV can be 'development', 'production', or 'test'
NODE_ENV=development
